{% extends 'Admin/admin_base.html.twig' %}

{% block title %}Carte{% endblock %}

{% block body %}
    <h1>Carte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carte.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ carte.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('carte_index') }}">Retour</a>

    <a href="{{ path('carte_edit', {'id': carte.id}) }}">Editer</a>

    {{ include('carte/_delete_form.html.twig') }}
{% endblock %}
