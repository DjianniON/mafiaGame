{% extends 'base.html.twig' %}

{% block title %}Tournoi{% endblock %}

{% block body %}
    <h1>Tournoi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournoi.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ tournoi.image }}</td>
            </tr>
            <tr>
                <th>NbParticipants</th>
                <td>{{ tournoi.nbParticipants }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournoi_index') }}">back to list</a>

    <a href="{{ path('tournoi_edit', {'id': tournoi.id}) }}">edit</a>

    {{ include('tournoi/_delete_form.html.twig') }}
{% endblock %}
